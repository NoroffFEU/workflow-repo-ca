{
  "name": "workflow-repo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx tailwindcss -i ./css/input.css -o ./css/style.css --watch",
    "test:unit": "vitest",
    "test:e2e": "playwright test tests/playwright",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "e2e:debug": "playwright test --debug",
    "start": "live-server --port=5503"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@vitest/expect": "^2.1.6",
    "jsdom": "^25.0.1",
    "lint-staged": "^15.2.10",
    "live-server": "^1.2.2",
    "tailwindcss": "^3.4.12",
    "vitest": "^2.1.6"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  
    "lint-staged": {
      "*.js": [
        "prettier --write",
        "eslint --fix"
      ],
      "*.html": [
        "prettier --write"
      ]
    }
}
